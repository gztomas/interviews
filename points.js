// Get minimum distance

function Point(x, y) {
  this.x = x;
  this.y = y;
}

Point.prototypecalculateDistance

Point.getClosestPoint = function(points) {
  for (let i = 0; i < points.length; i++) {
    let point = arguments[i];

  }
};